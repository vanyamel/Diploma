{
  "name": "logicmath",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "concurrently --names \"server,client\" --prefix-colors \"blue,green\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "migrate": "npm run migrate --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/vanyamel/Diploma.git"
  },
  "private": true
}
